# ReCompress environment variables
# Copy to .env and fill in. NEVER commit .env (it's gitignored).

# DeepSeek (compressor = V4 Pro, solver = V4 Flash)
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com

# The Token Company / bear-1.1 (fill endpoint + auth scheme from their docs)
BEAR_API_KEY=
BEAR_BASE_URL=

# Anthropic (offline quality gate — eval-time only, not in hot path)
ANTHROPIC_API_KEY=

# Modal (optional, trophy stretch only)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
